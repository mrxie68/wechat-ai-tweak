#ifndef AIConfig_h
#define AIConfig_h

// ============================================================
//  微信 AI 助手 - 配置文件
//  改完这些以后 push 到 GitHub，Actions 会自动编译
// ============================================================

// API Key（DeepSeek 示例，去 https://platform.deepseek.com 申请）
#define kAIAPIKey @"sk-REMOVED"

// OpenAI 兼容接口地址（DeepSeek 默认）
#define kAIBaseURL @"https://api.deepseek.com"

// 模型名（deepseek-v4-flash / deepseek-v4-pro / gpt-4o-mini / glm-4-flash 等）
// 注意：deepseek-chat 已弃用，请用 v4 系列
#define kAIModel @"deepseek-v4-flash"

// 触发词：trigger 模式下，消息以这个词开头才会触发 AI
#define kAITrigger @"@AI"

// ============================================================
//  回复模式
// ============================================================
//
//  @"auto"    = 自动代替聊天：白名单会话里，别人发消息 AI 自动替你回复
//  @"trigger" = 手动触发：只有 @AI 开头的消息才回复
#define kAIReplyMode @"auto"

// 自动模式下的“思考延迟”（秒）：收到消息后先等这一段时间再请求 AI。
// 除此之外还有模拟打字时间（按回复字数计算），总时长更像真人。
#define kAIReplyDelaySeconds 0.8

// 自动模式下 AI 的系统提示词（AI 扮演的是你自己）
// 这是默认值；注入后可在微信里发“@AI 设置”修改，保存后立即生效、重启不丢
#define kAIAutoSystemPrompt @"你是微信用户本人，替 ta 和好友聊天，让对话看起来就是 ta 本人发的。\n【怎么说话】短句、口语化，一两句说完，别写小作文；随意不端着，偶尔说点陕西方言（啥、咋了、么、嘹咋咧、么麻达）；适当用“哈哈”和 emoji，不用 Markdown。\n【注意】图片/表情/视频看不到就直说，别编；点外卖、转账、发红包、发文件、截图这些做不到，绝不假装做了；不知道就说不知道；对方要敏感信息就拒绝；别暴露你是 AI。\n想到啥直接回，别磨蹭。"

// 默认聊天风格样本（没粘贴自己的记录前先用这套，贴合陕西口语习惯）
#define kAIStyleSamplesDefault @"参考我的说话习惯：短句为主，想到啥说啥，一般不啰嗦；常用“哈哈、嗯、行、对、啥、咋了”，偶尔冒句陕西话（嘹咋咧、么麻达）；语气随意不端着，很少长篇大论。"

// 每个会话最多保留最近多少条消息作为上下文
#define kAIMaxContextMessages 30

// trigger 模式下 AI 的系统提示词
#define kAISystemPrompt @"你是微信里的一位 AI 助手，用简体中文简洁、友好地回答问题。你无法执行任何现实操作（点外卖、转账、发红包、发文件、打电话、查物流、发截图等），遇到这类请求要明确说明做不到，不要假装已经完成。"

// ============================================================
//  隐私保护（建议保持默认的最小读取模式）
// ============================================================
//
// 1. 只有白名单会话里的消息才会被读取；白名单外一律不读、不存、不传。
// 2. 上下文只记录“插件注入之后”该会话的双向消息（内存保存，重启微信即清空），
//    不会扫描或读取其他聊天记录。
//
// 会话白名单：AI 只在这些会话里生效。
//   单聊填对方 wxid（如 wxid_abc123），群聊填群 id（如 12345@chatroom）
//   多个用英文逗号分隔；留空表示所有会话都可以触发。
#define kAIAllowedChats @""

// 日志前缀（排查问题用，不用改）
#define kAITweakLogPrefix @"[WeChatAI] "

// 插件版本（弹窗/状态命令会显示，方便确认手机上装的是不是最新编译）
#define kAITweakVersion @"0.11.2"

#endif
